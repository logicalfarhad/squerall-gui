akka {
  jvm-exit-on-fatal-error = false
}

# Path to config file (where data access options are stored)
sourcesConfFile = "/conf/"

# Path to mappings database
mappingsDB = "/mappings.db/"

# Path to where schema-tools jar is stored (see documentations)
pathToParquetToolsJa r= "/home/sali/parquet-tools-1.10.1.jar"

# Unique secret key, can be generated using `sbt playGenerateSecret`
play.http.secret.key = "UtdjTwV32Dus<E1UDs:Xv]VE]UENJvJ7U9ei@L@MIb/`iG^ozB_oBLs3z>kpotg2"
play.http.secret.key = ${?APPLICATION_SECRET}

# 
play.filters.enabled += "play.filters.csrf.CSRFFilter"
play.filters.enabled += "play.filters.headers.SecurityHeadersFilter"
play.filters.enabled += "play.filters.hosts.AllowedHostsFilter"